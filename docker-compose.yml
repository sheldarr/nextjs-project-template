version: "3.5"

services:
  nextjs-project-template:
    image: node:12
    command: yarn dev
    working_dir: /app
    env_file:
      - .env
    ports:
      - ${APP_PORT}:${APP_PORT}
    volumes:
      - .:/app
